diff -uNr apache-couchdb-1.7.2.orig/src/couchdb/priv/couch_js/http.c apache-couchdb-1.7.2/src/couchdb/priv/couch_js/http.c
--- apache-couchdb-1.7.2.orig/src/couchdb/priv/couch_js/http.c	2018-06-17 11:29:47.000000000 +0200
+++ apache-couchdb-1.7.2/src/couchdb/priv/couch_js/http.c	2025-06-03 22:17:16.134566304 +0200
@@ -443,8 +443,8 @@
                                         (curl_seek_callback) seek_body);
         curl_easy_setopt(HTTP_HANDLE, CURLOPT_HEADERFUNCTION, recv_header);
         curl_easy_setopt(HTTP_HANDLE, CURLOPT_WRITEFUNCTION, recv_body);
-        curl_easy_setopt(HTTP_HANDLE, CURLOPT_NOPROGRESS, 1);
-        curl_easy_setopt(HTTP_HANDLE, CURLOPT_IPRESOLVE, CURL_IPRESOLVE_V4);
+        curl_easy_setopt(HTTP_HANDLE, CURLOPT_NOPROGRESS, (long)1);
+        curl_easy_setopt(HTTP_HANDLE, CURLOPT_IPRESOLVE, (long)CURL_IPRESOLVE_V4);
         curl_easy_setopt(HTTP_HANDLE, CURLOPT_ERRORBUFFER, ERRBUF);
         curl_easy_setopt(HTTP_HANDLE, CURLOPT_COOKIEFILE, "");
         curl_easy_setopt(HTTP_HANDLE, CURLOPT_USERAGENT,
@@ -474,22 +474,22 @@
     }
 
     curl_easy_setopt(HTTP_HANDLE, CURLOPT_CUSTOMREQUEST, METHODS[http->method]);
-    curl_easy_setopt(HTTP_HANDLE, CURLOPT_NOBODY, 0);
-    curl_easy_setopt(HTTP_HANDLE, CURLOPT_FOLLOWLOCATION, 1);
-    curl_easy_setopt(HTTP_HANDLE, CURLOPT_UPLOAD, 0);
+    curl_easy_setopt(HTTP_HANDLE, CURLOPT_NOBODY, (long)0);
+    curl_easy_setopt(HTTP_HANDLE, CURLOPT_FOLLOWLOCATION, (long)1);
+    curl_easy_setopt(HTTP_HANDLE, CURLOPT_UPLOAD, (long)0);
     
     if(http->method == HEAD) {
-        curl_easy_setopt(HTTP_HANDLE, CURLOPT_NOBODY, 1);
-        curl_easy_setopt(HTTP_HANDLE, CURLOPT_FOLLOWLOCATION, 0);
+        curl_easy_setopt(HTTP_HANDLE, CURLOPT_NOBODY, (long)1);
+        curl_easy_setopt(HTTP_HANDLE, CURLOPT_FOLLOWLOCATION, (long)0);
     } else if(http->method == POST || http->method == PUT) {
-        curl_easy_setopt(HTTP_HANDLE, CURLOPT_UPLOAD, 1);
-        curl_easy_setopt(HTTP_HANDLE, CURLOPT_FOLLOWLOCATION, 0);
+        curl_easy_setopt(HTTP_HANDLE, CURLOPT_UPLOAD, (long)1);
+        curl_easy_setopt(HTTP_HANDLE, CURLOPT_FOLLOWLOCATION, (long)0);
     }
     
     if(body && bodylen) {
         curl_easy_setopt(HTTP_HANDLE, CURLOPT_INFILESIZE, bodylen);        
     } else {
-        curl_easy_setopt(HTTP_HANDLE, CURLOPT_INFILESIZE, 0);
+        curl_easy_setopt(HTTP_HANDLE, CURLOPT_INFILESIZE, (long)0);
     }
 
     // curl_easy_setopt(HTTP_HANDLE, CURLOPT_VERBOSE, 1);
